<UserControl x:Class="TLCGen.Views.ControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TLCGen.Views"
             xmlns:vm="clr-namespace:TLCGen.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl SelectedItem="{Binding Path=SelectedTab}" SelectedIndex="{Binding Path=SelectedTabIndex}">
            <TabItem Header="Algemeen" Name="AlgemeenTab">
                <local:ControllerDataView DataContext="{Binding Path=ControllerDataVM}" />
            </TabItem>
            <TabItem Header="Fasen" Name="FasenTab">
                <local:FasenTab DataContext="{Binding Path=FasenTabVM}" />
            </TabItem>
            <TabItem Header="Conflicten" Name="ConflictenTab">
                <local:CoordinatiesTab DataContext="{Binding Path=ConflictMatrixVM}" />
            </TabItem>
            <TabItem Header="Detectoren" Name="DetectorenTab">
                <local:DetectorenTab DataContext="{Binding Path=DetectorenTabVM}" />
            </TabItem>
            <TabItem Header="Modules" Name="ModulesTab">
                <local:ModulesTab DataContext="{Binding Path=ModulesTabVM}" />
            </TabItem>
            <TabItem Header="Bitmap" Name="BitmapTab" IsEnabled="{Binding BitmapTabEnabled}">
                <local:BitmapTab DataContext="{Binding Path=BitmapTabVM}" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
