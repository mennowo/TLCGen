<UserControl x:Class="TLCGen.Views.ControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TLCGen.Views"
             xmlns:vm="clr-namespace:TLCGen.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl SelectedItem="{Binding Path=SelectedTab}" SelectedIndex="{Binding Path=SelectedTabIndex}">
            <TabItem Header="Algemeen">
                <local:ControllerDataView DataContext="{Binding Path=ControllerDataVM}" />
            </TabItem>
            <TabItem Header="Fasen">
                <local:FasenLijstView DataContext="{Binding Path=FasenListVM}" />
            </TabItem>
            <TabItem Header="Conflicten">
                <local:ConflictMatrixView DataContext="{Binding Path=ConflictMatrixVM}" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
